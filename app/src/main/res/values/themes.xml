<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.YogaLyze" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/pink</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:textColorPrimary">@color/font_black</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.YogaLyze" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="DialogTheme" parent="Theme.AppCompat.Dialog">
        <!-- Atur atribut-atribut yang ingin Anda sesuaikan -->
        <item name="android:textColorPrimary">@color/font_black</item>
        <item name="android:textColorSecondary">@color/font_black</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowMinWidthMinor">80%</item>
        <item name="android:windowMinWidthMajor">80%</item>
        <item name="android:buttonBarButtonStyle">@style/Button.Dialog</item>
        <!-- Tambahkan atribut-atribut lainnya sesuai kebutuhan -->
    </style>

    <style name="Button.Dialog" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_bg_pink</item>
        <item name="android:textColor">@color/white</item>
        <!-- Tambahkan atribut-atribut lainnya sesuai kebutuhan -->
    </style>

</resources>